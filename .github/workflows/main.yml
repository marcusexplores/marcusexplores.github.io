name: Deploy Main Project

on:
  push:
    branches: ['main']
  workflow_dispatch:

jobs:
  call-workflow:
    uses: marcusexplores/.github/workflows/deploy-pages.yml@main
    with:
      path: './dist'
    secrets: inherit
